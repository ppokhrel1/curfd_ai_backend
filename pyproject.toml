[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]
pythonpath = "."
# This line is critical for your async tests to work without @pytest.mark.asyncio decorators everywhere
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

